apple apps?

Chapter 3.
is remote attestation mandatory?
why we need TEE for code protection.
	reading: cloud privacy paper.

Chapter 5: 
	variation of designs?
	explore other possibiltes for code protection

Root detection kit vs remote attestation ?
app integrity , device integrity 

related work on remote attestation android 

model fixes:
- make app info reading out of attacker control (app cannot say I'm app number x ..etc) 
- public channels for messages between app and developer


Let OS_attestation(…) =
	in(RA_COMMAND, nonce,…)
	Read LogTable(boot_instance, xApp, …)
	output signature of (xApp, nonce, etc…)

Let OS = …
	OS_protocol1(…)
	| OS_protocol2(…)
	| OS_attestation(…)

Let UserSpaceProtocol1(xApp) =
	if xApp = APP_LEGITIMATE then
		… // legitimate app operations
	else
		… // adversary-controlled application (probably we simply forward messages from the adversary)

Let UserSpace( ….  xApp) =
	| UserSpaceProtocol1(xApp…)
	…


//Main process
!(
// Platform boots
new boot_instance;

in(xApp:application_source); 
Insert into LogTable(boot_instance, xApp, …..)


New x, y, ….

!OS(x,y …) | !UserSpace(a, b, c,  xApp)

)